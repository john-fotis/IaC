http:
  middlewares:
    chain-no-auth-webos:
      chain:
        middlewares:
          #- traefik-bouncer # leave this out if you are not using CrowdSec
          - rate-limit
          - secure-headers-webos
